---
trigger: always_on
---

Run the web server using uv `uv run -m uvicorn app.main:app --reload` or `/config/mnt/vault/@work-station/Python/FinComp/.venv/bin/python -m uvicorn app.main:app --reload`
